//módulos e require
//ao invés de import pode declarar variável os usando o require
const os = require('os');

// console.log(os.version());
//machine - processador
// console.log(os.machine());
// console.log(os.release());
//um mesmo processador tem várias cpus
// console.log(os.cpus());
// console.log(os.cpus()[0].model);

const fs = require('fs');

console.log(fs);
